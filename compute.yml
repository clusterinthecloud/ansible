---
- hosts: compute
  roles:
    - dhcp
    - filesystem
    - ssh
    - epel
    - security-updates
    - ntp
    - sssd
    - monitoring
    - slurm
  tasks:
    - name: disable SELinux
      selinux:
        state: disabled
